# PulseConfig.cmake - generated from PulseConfig.cmake.in

@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

set(Pulse_VERSION "@PROJECT_VERSION@")

set(Pulse_CONFIG_DIR "${CMAKE_CURRENT_LIST_DIR}")

set_and_check(Pulse_INCLUDE_DIR "${PACKAGE_PREFIX_DIR}/@PACKAGE_INCLUDE_INSTALL_DIR@")

set(PULSE_INCLUDE_DIRS "${Pulse_INCLUDE_DIR}")
set(PULSE_VERSION       "${Pulse_VERSION}")

include("${CMAKE_CURRENT_LIST_DIR}/PulseTargets.cmake")

set(Pulse_MODULE_DIR "${CMAKE_CURRENT_LIST_DIR}")
list(APPEND CMAKE_MODULE_PATH "${Pulse_MODULE_DIR}")

function(pulse_include_warnings)
  include("${CMAKE_CURRENT_LIST_DIR}/compiler_warnings.cmake")
endfunction()

if (NOT DEFINED CMAKE_CXX_STANDARD)
  set(CMAKE_CXX_STANDARD 20)
  set(CMAKE_CXX_STANDARD_REQUIRED ON)
endif()

if (DEFINED PULSE_CONFIG_VERBOSE AND PULSE_CONFIG_VERBOSE)
  message(STATUS "Found Pulse ${Pulse_VERSION}")
  message(STATUS "  Includes: ${Pulse_INCLUDE_DIR}")
endif()
